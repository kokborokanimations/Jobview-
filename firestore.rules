rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow read/write for all collections to allow the client SDK and backend sync to function without permission errors.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
